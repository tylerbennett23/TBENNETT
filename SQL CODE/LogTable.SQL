CREATE TABLE CW1. TrailAdditionLog (
	LogID INT IDENTITY(1,1) PRIMARY KEY,
	TrailID INT NOT NULL,
	AddedBy VARCHAR (100) NOT NULL,
	AddedTimestap DATETIME DEFUALT GETDATE(),
	FOREIGN KEY (TrailID) REFERENCES dbo.tRAILS(TrailID)
);